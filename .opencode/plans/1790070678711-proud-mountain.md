# Over-engineering audit (ponytail-audit, whole tree)

Read-only scan. Findings only; nothing applied. Ranked biggest cut first.

## Findings

`delete` ~700-line dead colour-science API (oklab_to_srgb, xyz/lch, okhsv/okhsl, gamut, 4 nn.Module wrappers; keep ~105-line forward path only). Call `srgb_to_oklab` path alone. [src/convgamer/data/oklab.py] (-700)

`delete` taichi stack: kernels/ (empty shells + runtime.py), callbacks/taichi_init.py, engine `_taichi_init_callback` entry, taichi dep — zero kernels, no trainer config uses it. torch does compute. [src/convgamer/kernels/, src/convgamer/callbacks/taichi_init.py, src/convgamer/training/engine.py, pyproject.toml] (-58, -1 dep)

`delete` `jepa_tiny` config family — value-identical to `jepa` (diff = comments / dirpath). CLI overrides. [configs/{model,data,trainer,experiment}/jepa_tiny*] (-149)

`delete` MinConvLSTM — only MinConvExpLSTM used in prod; second variant test-only. [src/convgamer/models/convgamer/minconv.py] (-108)

`delete` notebooks/ + notebooks extra (jupyter, jupytext, ipykernel, nbconvert) — one 27-line percent script, no .ipynb. [notebooks/, pyproject.toml] (-61, -4 pkgs)

`yagni` models/protocols.py — Protocol with one implementor; all uses are no-op `cast`. Type as nn.Module. [src/convgamer/models/protocols.py] (-49)

`shrink` merge RandomImageDataset/RandomVideoDataset — pure num_frames setters over RandomSyntheticDataset. One class, optional num_frames. [src/convgamer/data/dataset.py] (-40)

`shrink` blocks.py re-export shim — post-split alias layer; only tests import via it. Point tests at sources, delete shim. [src/convgamer/models/convgamer/blocks.py] (-39)

`yagni` experiment/ config group — only restates seed/fast_dev_run; `cfg.seed` never read (`seed_everything` absent from src). [configs/experiment/*] (-24)

`yagni` BaseModel — "carries no shared implementation"; two subclasses inherit nn.Module. [src/convgamer/models/base.py] (-22)

`delete` uniform_temporal_subsample — tests-only. [src/convgamer/models/convgamer/downsampler.py] (-21)

`yagni` ConvGamerLogger — re-logs val metrics PL already logs. [src/convgamer/callbacks/logging.py] (-19)

`delete` auto_mapping.py — `register_auto_classes()` never imported by src/tests/export_hf. [src/convgamer/integrations/transformers/auto_mapping.py] (-16)

`delete` unused deps: numpy, safetensors, tqdm — zero imports in src/tests. [pyproject.toml] (-3 deps)

`yagni` use_softmax ablation on ConvGamerStem — every config false; drop branch. [src/convgamer/models/convgamer/stem.py] (-15)

`shrink` JEPALoss.compute_context_lambdas — thin wrapper over module fn used only internally. Call fn. [src/convgamer/models/jepa/loss.py] (-13)

`shrink` build_datamodule branches — 8 identical kwargs duplicated; one kwargs dict. [src/convgamer/scripts/common.py:154-181] (-13)

`yagni` predictor num_levels heads — always 1; forward uses heads[-1] only. Single conv. [src/convgamer/models/jepa/predictor.py] (-12)

`delete` dead re-exports (_expand_archive_globs etc) + oklab_convert_srgb alias. Call srgb_to_oklab. [src/convgamer/data/dataset.py:8-16] (-12)

`yagni` ConvGamerModel pure-delegation wrappers (forward_features/init_state/step) — callers use backbone. [src/convgamer/modules/lightning_module.py] (-11)

`delete` concat_original + depthwise flags on LearnedSpatialTemporalDownsampler — never set. [src/convgamer/models/convgamer/downsampler.py] (-10)

`delete` inception_tiny.yaml — identical to inception_next.yaml. [configs/model/inception_tiny.yaml] (-10)

`delete` tracked refactor cruft: .refactor-audit.md, .refactor-plan.md, .omp plan file — plan itself says remove temp files. [., .omp/] (-~700 lines tracked docs)

`delete` .serena/ tracked IDE state (project.yml). [.serena/] (-169)

`delete` .pre-commit-config.yaml + pre-commit dep — not enforced by CI; hooks duplicate plain ruff. [.pre-commit-config.yaml, pyproject.toml] (-1 dep)

`shrink` GameVideoDataset._load_video start/stride — nested ternary + pad; need_pad/start form. [src/convgamer/data/dataset.py:219-241] (-8)

`delete` archive_dataset_ids config key — never read (one length assert only). [configs/data/jepa*.yaml] (-8)

`delete` data/video.yaml — no `data=video` anywhere. [configs/data/video.yaml] (-8)

`delete` debug/ group — never composed; fast_dev_run read from root cfg anyway. [configs/debug/fast.yaml] (-4)

`native` module-level decord try/except stub — GameVideoDataset already local-imports; fail-fast. [src/convgamer/data/dataset.py:22-27] (-6)

`delete` data/__init__.py barrel — zero `from convgamer.data import` consumers. [src/convgamer/data/__init__.py] (-27)

`delete` tests/ops/ empty package. [tests/ops/] (-1)

`delete` ClassificationLoss + accuracy() test helpers — self-declared no production consumer; 5 tests test the test code. [tests/test_shared_seams.py:62-96] (-35)

`shrink` EMA callback covered twice (shared_seams vs wiring_seams) — merge to one wiring assert. [tests/test_shared_seams.py, tests/test_wiring_seams.py] (-8)

`delete` tests/__init__.py — 0 bytes; testpaths already set. [tests/__init__.py] (-0)

`delete` pyrefly.toml — empty; real config in pyproject [tool.pyrefly]. [pyrefly.toml] (-0)

`delete` benchmarks/ — placeholder README, zero refs. [benchmarks/] (-3)

`yagni` HF forward multimodal baggage — input_ids/inputs_embeds/Identity embeddings; only pixel_values used. [src/convgamer/integrations/transformers/modeling_convgamer.py] (-12)

`yagni` dead trainer/optimizer metadata — `target:` never read ×3; `optimizer.target` never instantiated; `betas` never passed. [configs/trainer/*.yaml, configs/optimizer/*.yaml] (-5)

`yagni` regularization_dataset_ids list — only `[0]` used; single str. [src/convgamer/data/jepa_datamodule.py:104-107] (-5)

`delete` total_steps param — stored, never read (scheduler uses warmup_steps). [src/convgamer/modules/jepa_module.py, configs] (-5)

`delete` EncoderT/Generic export — no external use. [src/convgamer/modules/ema.py, modules/__init__.py] (-5)

`delete` JEPALoss.feature_dim — stored, never used. [src/convgamer/models/jepa/loss.py, configs] (-4)

`delete` use_norm flag on stem — encoder hardcodes False. [src/convgamer/models/convgamer/stem.py] (-4)

`delete` early_stopping engine entry — no trainer config lists it. [src/convgamer/training/engine.py] (-3)

`delete` CausalTemporalMixer unread attrs (channels/dilations/receptive_field). [src/convgamer/models/convgamer/causal.py] (-3)

`shrink` duplicate stem registration (downsampler/spatial_stem/stem Sequential aliases same modules). [src/convgamer/models/convgamer/encoder.py] (-3)

`delete` _version.py — duplicates pyproject version; use importlib.metadata. [src/convgamer/_version.py] (-4)

`shrink` private-attr test `_effective_lambda` — assert public loss behavior. [tests/models/test_jepa_loss.py:22-24] (-3)

## Not cuts (verified in use)

training/engine.py, models/io.py, data/sources.py, GameImageDataset, HFVideoDataset, RandomVideoDataset (wiring tests), srgb_to_oklab, scripts/common builders, HF integration modeling/configuration, data/default.yaml, einops.

Untracked local noise (gitignored, no action): .refactor-baseline.log, .refactor-lint-baseline.log, skills-lock.json, dist/.

## Net

`net: ~-2100 lines, -9 deps possible (taichi, numpy, safetensors, tqdm, pre-commit, + notebooks extra: jupyter/jupytext/ipykernel/nbconvert).`

## Execution plan (if applying cuts)

1. Delete dead modules/flags: oklab dead half, taichi stack, MinConvLSTM, protocols.py, base.py BaseModel collapse, auto_mapping.py, blocks.py shim (+ retarget tests), dataset dead re-exports, logging.py callback (+ config), engine early_stopping, _version.py (switch export_hf to importlib.metadata).
2. Config collapse: drop jepa_tiny family, inception_tiny, experiment/, debug/, data/video.yaml, archive_dataset_ids, dead trainer/optimizer keys; fix 2–4 tests referencing them.
3. pyproject: remove taichi/numpy/safetensors/tqdm/pre-commit + notebooks extra; drop .pre-commit-config.yaml; delete tracked cruft (.refactor-*, .omp plan, .serena/, pyrefly.toml, benchmarks/, tests/ops/, tests/__init__.py).
4. Shrink: merge Random* datasets, build_datamodule kwargs, _load_video ternary, JEPALoss wrapper, predictor heads, stem flags, dataset alias merges, seam test dedup.
5. Verify: `uv run ruff check . && uv run ruff format --check . && uv run pyrefly check && uv run pytest` (gates per testing.md; report if env missing).
